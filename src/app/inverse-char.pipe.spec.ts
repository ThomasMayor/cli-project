import { InverseCharPipe } from './inverse-char.pipe';

describe('InverseCharPipe', () => {
  it('create an instance', () => {
    const pipe = new InverseCharPipe();
    expect(pipe).toBeTruthy();
  });
});
